# Example selection config: pin one saved model per role for eval / downstream use.
store:
  root: models
roles:
  demo:
    target: demo
    method: XGB
    feature_hash:        # leave blank to use models/demo/_latest.json; or pin a hash
    train_start: 2024-01-01
    train_end:   2024-03-01
